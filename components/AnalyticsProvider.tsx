"use client";

export default function AnalyticsProvider() {
  // First App Store release: no account flow and no app analytics/heartbeat.
  // Keep the provider mounted as a stable integration point for future releases.
  return null;
}

